package cs2030.simulator;

/** This enum represents all the possible states of events.
 */
public enum State {
    ARRIVES, SERVED, WAITS, WAITING, LEAVES, DONE,
    SERVER_REST, SERVER_BACK;
}                         
